name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release
  cancel-in-progress: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  build:
    name: Build ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            label: linux-amd64
            test: true

          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            label: linux-arm64
            cross: true

          - target: x86_64-apple-darwin
            os: macos-13
            label: darwin-amd64

          - target: aarch64-apple-darwin
            os: macos-14
            label: darwin-arm64

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      # Quick smoke test only on linux-amd64 (CI already validates on push)
      - name: Run tests
        if: matrix.test
        run: cargo test --workspace

      - name: Install cross-compiler (Linux ARM64)
        if: matrix.cross
        run: sudo apt-get update -qq && sudo apt-get install -y -qq gcc-aarch64-linux-gnu

      - name: Build release binaries
        run: |
          cargo build --release --target ${{ matrix.target }} \
            --bin zo-tunnel-server --bin zo-tunnel-client

      - name: Package binaries
        run: |
          TAG="${GITHUB_REF_NAME}"
          for binary in zo-tunnel-server zo-tunnel-client; do
            BIN="target/${{ matrix.target }}/release/${binary}"
            if [ -f "$BIN" ]; then
              TAR="${binary}-${TAG}-${{ matrix.label }}.tar.gz"
              tar -czf "$TAR" -C "target/${{ matrix.target }}/release" "$binary"
              echo "📦 $TAR ($(du -h "$TAR" | cut -f1))"
            fi
          done

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: binaries-${{ matrix.label }}
          path: "*.tar.gz"
          retention-days: 1

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Generate checksums
        working-directory: dist
        run: sha256sum *.tar.gz > SHA256SUMS.txt

      - name: List release files
        run: ls -lh dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            dist/*.tar.gz
            dist/SHA256SUMS.txt
          body: |
            ## Install

            **Server (Linux VPS):**
            ```bash
            curl -sSL https://raw.githubusercontent.com/${{ github.repository }}/main/scripts/install.sh | sudo bash -s server
            zo-tunnel-server start --domain tunnel.example.com
            ```

            **Client (macOS / Linux):**
            ```bash
            curl -sSL https://raw.githubusercontent.com/${{ github.repository }}/main/scripts/install.sh | bash
            ```
